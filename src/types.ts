export interface Film {
  id: number;
  title: string;
  poster: string;
  director: string;
  year: number;
}

export interface NewFilm {
  title: string;
  poster: string;
  director: string;
  year: number;
}
